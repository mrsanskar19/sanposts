'use client';

import React from 'react';
import { SocialStudio } from '@/components/studio/SocialStudio';

export default function HomePage() {
  return <SocialStudio initialConversationId={null} />;
}